<% if @occurrence.errors.any? %>

  error_message = "";
  error_message = $("error_message");

  <% @comment.errors.full_messages.each do |error| %>
    error_message.append('<%= escape_javascript( error ) %>\n');
  <% end %>

  alert(error_message);

<% else %>

  
 /* popup_text = "JHSSHKHSHSJKL";/*"<%= escape_javascript( @occurrence.type.description ) %>" + "</br>"
  + "<%= escape_javascript( @occurrence.date.description ) %>" + "</br>"
  + "<%= escape_javascript( @occurrence.hour.description ) %>" + "</br>"
  + "Declarante:" + "<%= escape_javascript( @occurrence.declarant.name ) %>";*/

alert(popup_text); */

  L.marker(global_clicked_lat_lon).addTo(global_map_container).bindPopup("popup_text");

  // Clear form
  $('#new-occurrence-form').find('input:text,textarea').val('');
  $('#insert_here').hide();

<% end %>








